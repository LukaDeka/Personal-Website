<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
{% for entry in entries %}
  <url>
    <loc>{{ entry.permalink | safe }}</loc>
    {% if entry.updated %}
    <lastmod>{{ entry.updated }}</lastmod>
    {% endif %}
  </url>
  {% if entry.extra and entry.extra.additional_sitemap %}
  <url>
    <loc>{{ entry.extra.additional_sitemap | safe }}</loc>
    {% if entry.updated %}
    <lastmod>{{ entry.updated }}</lastmod>
    {% endif %}
  </url>
  {% endif %}
{% endfor %}
</urlset>

